{
  "name": "@cherry/pipeline",
  "version": "0.0.1",
  "private": true,
  "description": "TypeScript data pipeline scripts — content ingestion, Notion sync, GitHub publication",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.0",
    "@notionhq/client": "^2.3.0",
    "@octokit/rest": "^20.1.0",
    "node-cron": "^3.0.3",
    "p-throttle": "^5.0.0",
    "pg": "^8.13.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "nock": "^13.5.0",
    "vitest": "^2.0.0"
  }
}
